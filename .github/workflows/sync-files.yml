# This workflow will deploy the entire branch to our remote system.
# Uses rsync.
# It will run on dispatch, on push, on PR.
# See: https://zellwk.com/blog/github-actions-deploy/
#      https://github.com/marketplace/actions/rsync-deployments-action

name: Sync to remote

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  deploy:
    name: Remote deploymen
    runs-on: ubuntu-latest
    steps:
    - name: Checkout branch
      uses: actions/checkout@v3
    - name: Get branch name
      id: branch-name
      uses: tj-actions/branch-names@v6
    - name: Deploy
      uses: burnett01/rsync-deployments@5.2.1
      with:
        switches: -avzr --delete --exclude=".github" --exclude=".git"
        path: /
        remote_path: /home/azureuser/sito/${{ steps.branch-name.outputs.current_branch }}
        remote_host: ${{ secrets.SSH_HOSTNAME }}
        remote_port: ${{ secrets.SSH_PORT }}
        remote_user: ${{ secrets.SSH_USER }}
        remote_key:  ${{ secrets.SSH_PRIVATE_KEY }}
  accessibility-audit:
    name: Accessibility audit
    needs: deploy
    uses: ggardin/tecweb/.github/workflows/a11y-audit.yml@1
    with:
      target-branch: ${{ jobs.deploy.steps.branch-name.outputs.current_branch }}
  broken-links-check:
    name: Check broken links
    needs: deploy
    uses: ggardin/tecweb/.github/workflows/check-broken-links.yml@1
    with:
      target-branch: ${{ steps.branch-name.outputs.current_branch }}
  pagespeed-audit:
    name: PageSpeed Insights
    needs: deploy
    uses: ggardin/tecweb/.github/workflows/check-pagespeed-performance.yml@1
    with:
      target-branch: ${{ steps.branch-name.outputs.current_branch }}
